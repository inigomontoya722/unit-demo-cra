name: Release

on:
  pull_request:

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create Issue 
        run: cat RELEASE.md | github-issue create